# personal progress — every player keeps their own
progress.json

# python
__pycache__/
*.pyc
.venv/

# local working docs (never committed)
/plans/

# personal vault link (each player points at their own vault)
quest.config.json
